const express = require('express');
const router = express.Router();
// const authController = require('../controllers/authController');

// Define routes here, e.g., router.post('/login', authController.login);

module.exports = router;
